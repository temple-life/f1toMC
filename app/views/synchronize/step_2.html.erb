<div class="grid_16">
    <h2>
		<span class="mute">Synchronize &ndash;</span> Add to lists
	</h2>
    <%= form_tag synchronize_path, :method => :post do %>
        <table class="grid">
			<thead>
            	<tr>
	                <th class="shrink align_center">
						<a href="#" class="check_all">All</a>
					</th>
	                <th>
						Name
					</th>
	                <th class="align_right">
						Subscribers
					</th>
					<th class="align_right">
					    Campaigns
					</th>
	                <th class="align_right">
						Created
					</th>
	            </tr>
			</thead>
			<tbody>
	            <% @lists.each do |list| %>
	                <tr>
	                    <td class="shrink align_center">
	                        <input type="checkbox" name="lists[]" value="<%= list['id'] %>" />
	                    </td>
	                    <td>
	                        <%= list['name'] %>
	                    </td>
	                    <td class="align_right">
	                        <%= list['member_count'] || "0" %>
	                    </td>
	                    <td class="align_right">
	                        <%= list['campaign_count'] || "0" %>
	                    </td>
	                    <td class="align_right">
	                        <%= DateTime.parse(list['date_created']).strftime("%d %b %Y") %>
	                    </td>
	                </tr>
	            <% end %>
			</tbody>
        </table>
        <p>
            <%= submit_tag "Save people to list" %>
        </p>
    <% end %>
</div>